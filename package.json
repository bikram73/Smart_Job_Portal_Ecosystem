{
  "name": "smart-job-portal",
  "version": "1.0.0",
  "description": "Intelligent Job Portal Ecosystem",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install && cd ../api && npm install",
    "postinstall": "cd server && npm install && cd ../api && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "build:client": "cd client && npm run build",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "keywords": [
    "job-portal",
    "resume-builder",
    "job-tracker"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "concurrently": "^8.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
